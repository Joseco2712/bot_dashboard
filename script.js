function guardarConfiguracion() {
    const prefix = document.getElementById("prefix").value;
    alert(`Se guardaría el prefijo: ${prefix}`);
    // Aquí luego conectarías con un backend para guardar la configuración real
  }
  